<!DOCTYPE html>
<html lang="en">
	<head>
		<%- include('../partials/head.ejs') %>
		<title>Edit</title>
	</head>
	<body class="min-h-screen">
		<%- include('../partials/header.ejs') %>
		<main class="my-5">
			<h1 class="text-3xl text-center mb-5 font-medium">Edit <%= event.title %></h1>
			<form action="/events/<%= event._id %>?_method=PUT" enctype="multipart/form-data" method="POST" autocomplete="off">
				<div>
					<label for="category" class="block mb-1">Category <span class="text-red-700">*</span></label>
					<select class="border border-[#046a37] w-full p-1 rounded" name="category" id="category" required>
						<option value="meetups" <% if (event.category === 'meetup') { %>selected<% } %>>Meetup</option>
						<option value="study" <% if (event.category === 'study') { %>selected<% } %>>Study</option>
						<option value="gaming" <% if (event.category === 'gaming') { %>selected<% } %>>Gaming</option>
						<option value="food" <% if (event.category === 'food') { %>selected<% } %>>Food</option>
						<option value="miscellaneous" <% if (event.category === 'miscellaneous') { %>selected<% } %>>Miscellaneous</option>
					</select>
				</div>
				<div>
					<label for="title" class="block mb-1">Title <span class="text-red-700">*</span></label>
					<input
						class="border border-[#046a37] w-full p-1 rounded"
						type="text"
						name="title"
						id="title"
						value="<%= event.title %>"
						required
					/>
				</div>
				<div>
					<label for="details" class="block mb-1">Details <span class="text-red-700">*</span></label>
					<textarea
						class="border border-[#046a37] w-full p-1 rounded align-top"
						name="details"
						id="details"
						cols="30"
						rows="2"
						placeholder="Description"
						required
					><%= event.details %></textarea
					>
				</div>
				<div>
					<label for="location" class="block mb-1">Where <span class="text-red-700">*</span></label>
					<input
						class="border border-[#046a37] w-full p-1 rounded"
						type="text"
						name="location"
						id="location"
						value="<%= event.location %>"
						required
					/>
				</div>
				<div>
					<label for="start" class="block mb-1">Start <span class="text-red-700">*</span></label>
					<input
						class="border border-[#046a37] w-full p-1 rounded"
						type="datetime-local"
						name="start"
						id="start"
						value="<%= event.start %>"
						required
					/>
				</div>
				<div>
					<label for="end" class="block mb-1">End <span class="text-red-700">*</span></label>
					<input
						class="border border-[#046a37] w-full p-1 rounded"
						type="datetime-local"
						name="end"
						id="end"
						value="<%= event.end %>"
						required
					/>
				</div>
				<div>
					<label for="image" class="block mb-1">Image <span class="text-red-700">*</span></label>
					<input class="border border-[#046a37] w-full p-1 rounded" type="file" name="image" id="image" required/>
				</div>
				<button class="bg-[#046a37] hover:bg-green-900 w-full text-white p-2 rounded mt-4" type="submit">Submit</button>
			</form>
		</main>
		<%- include('../partials/footer.ejs') %>
	</body>
</html>
